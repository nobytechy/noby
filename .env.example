VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
VITE_SITE_URL=https://noby.aizim.co.zw

# Server-only — used by netlify/functions/chat.js (Ask Noby bot).
# DO NOT prefix with VITE_ — that would expose it to the browser.
# Get this from Supabase → Project Settings → API → service_role secret.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
